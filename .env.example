# ============================================================
# Blue Next Projet -- Environment Variables
# ============================================================
#
# SETUP:
#   1. Copy this file to .env.local:  cp .env.example .env.local
#   2. Fill in the values below with your actual credentials
#   3. NEVER commit .env.local to git (it is already in .gitignore)
#
# See DEPLOYMENT.md for detailed instructions on where to find
# each of these values.
# ============================================================

# --------------------------------------------------
# SUPABASE
# --------------------------------------------------
# Found at: Supabase Dashboard > Settings > API

# Your Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Your Supabase anonymous/public API key (safe to expose in browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Your Supabase service role key (KEEP SECRET -- full database access)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# --------------------------------------------------
# STRIPE
# --------------------------------------------------
# Found at: Stripe Dashboard > Developers > API Keys
# Use test keys (sk_test_ / pk_test_) in development
# Use live keys (sk_live_ / pk_live_) in production

# Stripe secret key (KEEP SECRET)
STRIPE_SECRET_KEY=sk_test_your-secret-key-here

# Stripe publishable key (safe to expose in browser)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your-publishable-key-here

# Stripe webhook signing secret
# Found at: Stripe Dashboard > Developers > Webhooks > your endpoint > Signing secret
# For local dev: run `stripe listen --forward-to localhost:3000/api/webhooks/stripe`
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret-here

# --------------------------------------------------
# RESEND (Email) -- OPTIONAL
# --------------------------------------------------
# If not set, the app still works -- email features are disabled.
# Found at: https://resend.com/api-keys

# Resend API key
RESEND_API_KEY=re_your-api-key-here

# The "from" email address for outgoing emails
# Must be on a domain you have verified in Resend
FROM_EMAIL=noreply@yourdomain.org

# The sender name that appears in email clients
FROM_NAME=Blue Next Projet

# --------------------------------------------------
# APP CONFIGURATION
# --------------------------------------------------

# Your site's full URL (no trailing slash)
# Local development: http://localhost:3000
# Production: https://yourdomain.com or https://your-app.vercel.app
NEXT_PUBLIC_APP_URL=http://localhost:3000

# --------------------------------------------------
# ADMIN
# --------------------------------------------------

# The email address of the admin user
# This must match the user you create in Supabase Authentication
# Also used as the recipient for contact form submissions
ADMIN_EMAIL=admin@yourdomain.org
